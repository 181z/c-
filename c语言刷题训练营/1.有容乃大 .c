#include <stdio.h>

int main() {
   pritnf("The size of short is %d bytes",sizeof(short));
   pritnf("The size of int is %d bytes",sizeof(int));
   pritnf("The size of long is %d bytes",sizeof(long));
   pritnf("The size of long long is %d bytes",sizeof(long long));
   return 0;
}